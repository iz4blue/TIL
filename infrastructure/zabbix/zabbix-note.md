## zabbix agent active 기준
- agent 가 server 로 보내면 active mode
- zabbix-proxy 역시 proxy 서버가 zabbix-server 로 먼저 데이터를 보내면 active ZBX_PROXYMODE=0

## zabbix-proxy 버전
- zabbix-proxy 는 zabbix server 와 버전이 동일해야 함
- zabbix agent 와는 zabbix-proxy 가 버전이 높으면 됨

